# FedAvg sync smoke test: 10 trainers, homogeneous (alpha=100), no failures.
#
# Run:
#   python -m flame.launch.run_experiment \
#       lib/python/examples/async_cifar10/expt_scripts_2026/fedavg_n10_alpha100_smoke.yaml

experiments:
  - name: fedavg_n10_alpha100_smoke
    description: "FedAvg sync smoke (10 trainers, alpha=100, syn_0)."

    baseline: fedavg

    trainer:
      num_trainers: 10
      start_id: 1
      dataset:
        name: cifar10
        dirichlet_alpha: 100.0
      availability:
        mode: syn_0
      enable_training_delays: true
      speedup_factor: 1.0
      hyperparameters:
        batchSize: 32
        learningRate: 0.001

    aggregator:
      config_template: ../_metadata/aggregator_base.json
      selector: random
      tracking_mode: default
      agg_goal: 5
      log_to_wandb: false
      config_overrides:
        job:
          id: fedavg_n10_alpha100_smoke
        hyperparameters:
          batchSize: 32
          learningRate: 0.001
          rounds: 200
        selector:
          kwargs:
            k: 5

    execution:
      num_gpus: 2
      sleep_between_spawns: 1.0
      aggregator_warmup_time: 15
      monitoring:
        enabled: false
